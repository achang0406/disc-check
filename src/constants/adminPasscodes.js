/** Default platform passcode (landing → Add group). Seeded to app_config. */
export const DEFAULT_PLATFORM_ADMIN_PASSCODE = "6789";

/** Default passcode for new groups and db:seed group rows. */
export const DEFAULT_GROUP_ADMIN_PASSCODE = "0000";
